/* global moment */

angular.module('track.service', ['ngResource'])
  .factory('simpleService', function() {
    return {
      // inDay: function(durationInHour) {
      //   return (durationInHour / 24.0).toFixed(1);
      // }
    };
  });
